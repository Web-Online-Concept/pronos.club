export const metadata = {
  title: "Mes Martingales — PRONOS.CLUB",
  robots: { index: false, follow: false },
};

export default function MartingalesLayout({ children }: { children: React.ReactNode }) {
  return children;
}